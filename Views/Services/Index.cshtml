@model List<manage_my_hairsaloon.Models.Service>
@{
    ViewData["Title"] = "Services";
}

<div class="page-header">
    <h1>Services</h1>
</div>

<div class="filter-bar" style="display:flex; gap:0.5rem; flex-wrap:wrap; margin-bottom:1rem;">
    <a href="/Services/Index" class="btn-outline-brass">All</a>
    <a href="/services/category/HairCut" class="btn-outline-brass">HairCut</a>
    <a href="/services/category/Coloring" class="btn-outline-brass">Coloring</a>
    <a href="/services/category/Styling" class="btn-outline-brass">Styling</a>
    <a href="/services/category/Treatment" class="btn-outline-brass">Treatment</a>
    <a href="/services/category/Beard" class="btn-outline-brass">Beard</a>
    <a href="/services/category/Nails" class="btn-outline-brass">Nails</a>
    <a href="/services/category/FacialTreatment" class="btn-outline-brass">FacialTreatment</a>
</div>

<div class="table-responsive">
    <table class="table-barbershop">
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Duration</th>
                <th>Category</th>
                <th>Salon</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var service in Model)
            {
                <tr>
                    <td>@service.Name</td>
                    <td>@(service.Description?.Length > 60 ? service.Description.Substring(0, 60) + "…" : service.Description)</td>
                    <td>@service.Price.ToString("C")</td>
                    <td>@service.DurationMinutes min</td>
                    <td><span class="badge-brass">@service.Category</span></td>
                    <td>@service.HairSalon?.Name</td>
                    <td>
                        <a asp-controller="Services" asp-action="Details" asp-route-id="@service.Id" class="btn-outline-brass" style="font-size:0.72rem; padding:0.25rem 0.7rem;">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
